type Query {
  info: String!
  feed: [Link!]!
  link(id: ID!): Link!
}

type Mutation {
  post(url: String!, description: String!): Link!
  signUp()
}

type Link {
  id: ID!
  description: String!
  url: String!
}